services:
  face-cropper:
    build: .
    ports:
      - "127.0.0.1:35013:8000"
    volumes:
      - ./original:/app/original
      - ./character:/app/character
      - ./crops:/app/crops
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
